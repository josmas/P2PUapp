cache access to the API 
cache access to the API or mock calls (recording?) for development
detect on main page if wifi is on, and prompt if not.
can I link ant to jshint?
find a way to 'yield' content and have a main page to embed the others
add error handling when get_JSON fails silently



Writeable API:
One story could be modify your profile
  - Authorisation
  - Authentication
  - GET profile (users) data
  - PUT data
